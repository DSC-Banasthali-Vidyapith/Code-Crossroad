name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Hi 😄, thanks for creating issue at Code Crossroad, do read and follow the [Code of Conduct](https://github.com/) and the [Contribution Guidelines](https://github.com/) while contributing.'
        
        
        
